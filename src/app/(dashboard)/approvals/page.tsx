import { PlaceholderPage } from "@/components/placeholder-page";

export default function ApprovalsPage() {
  return (
    <PlaceholderPage
      title="Approvals"
      phaseLabel="Phase 4"
      description="Pending / Approved / Rejected / Failed actions — the two-stage Worker → Head → CEO review chain, requiring explicit sign-off before anything executes."
    />
  );
}
