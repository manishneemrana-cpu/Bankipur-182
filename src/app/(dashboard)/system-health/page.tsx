import { PlaceholderPage } from "@/components/placeholder-page";

export default function SystemHealthPage() {
  return (
    <PlaceholderPage
      title="System Health"
      phaseLabel="Phase 5+"
      description="Status of Database, AI Provider, n8n, SitesNSign Connector, Meta, WhatsApp, Calling, Calendar, and Email integrations."
    />
  );
}
