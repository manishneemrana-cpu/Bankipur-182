import { PlaceholderPage } from "@/components/placeholder-page";

export default function AgentsPage() {
  return (
    <PlaceholderPage
      title="Agent Registry"
      phaseLabel="Phase 3"
      description="The CEO Agent, 4 Head Agents (CREO, CMO, CAO, CFLO), and their Worker Agents — with the Standard Agent Output Contract."
    />
  );
}
