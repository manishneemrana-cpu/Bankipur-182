import { PlaceholderPage } from "@/components/placeholder-page";

export default function AiCostPage() {
  return (
    <PlaceholderPage
      title="AI Cost Monitoring"
      phaseLabel="Phase 3+"
      description="Per-request, per-agent, per-department AI spend with configurable limits that auto-stop non-essential calls and notify the admin."
    />
  );
}
