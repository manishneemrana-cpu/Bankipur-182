import { Contact } from "lucide-react";
import { PhaseStub } from "../phase-stub";

export default function ContactsPage() {
  return (
    <PhaseStub
      title="Contacts"
      icon={Contact}
      description="Contact list, tags, custom fields, import/export, and opt-in/opt-out tracking ship in Phase 6."
    />
  );
}
