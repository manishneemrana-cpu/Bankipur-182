import { Settings } from "lucide-react";
import { PhaseStub } from "../phase-stub";

export default function SettingsPage() {
  return (
    <PhaseStub
      title="Settings"
      icon={Settings}
      description="White-label branding (name, logo, colors, favicon) and organization settings ship in a later phase."
    />
  );
}
