import { Users } from "lucide-react";
import { PhaseStub } from "../phase-stub";

export default function TeamPage() {
  return (
    <PhaseStub
      title="Team"
      icon={Users}
      description="Inviting team members and managing OWNER/ADMIN/MANAGER/AGENT/VIEWER roles ships alongside the shared inbox in Phase 6."
    />
  );
}
