import { PhaseStub } from "../phase-stub";

export default function InboxPage() {
  return (
    <PhaseStub
      title="Inbox"
      description="The shared team inbox (conversation list, thread view, customer profile, agent assignment) ships in Phase 6."
    />
  );
}
