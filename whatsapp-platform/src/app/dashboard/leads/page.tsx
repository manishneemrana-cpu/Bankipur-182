import { PhaseStub } from "../phase-stub";

export default function LeadsPage() {
  return (
    <PhaseStub
      title="Leads"
      description="The CRM (Kanban + table views, real-estate presets like property interest/budget/BHK/site-visit scheduling) ships in Phase 6."
    />
  );
}
