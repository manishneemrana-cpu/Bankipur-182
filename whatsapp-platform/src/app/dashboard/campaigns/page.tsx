import { Megaphone } from "lucide-react";
import { PhaseStub } from "../phase-stub";

export default function CampaignsPage() {
  return (
    <PhaseStub
      title="Campaigns"
      icon={Megaphone}
      description="Campaign builder, audience targeting, and the Compliance Guardian pre-flight check ship in Phase 7."
    />
  );
}
